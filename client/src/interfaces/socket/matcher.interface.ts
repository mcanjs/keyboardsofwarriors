export interface IMatcherFoundedData {
  queueLanguage: string;
  rank: number;
  roomId: string;
}

export interface IMatcherRoomData {
  queueLanguage: string;
  rank: number;
  roomId: string;
}

export interface IMatcherQueueBanData {
  banTime: string;
  serverTime: string;
}
